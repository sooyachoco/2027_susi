# Deployment trigger

This file intentionally records a fresh production deployment trigger for Cloudflare Workers Builds.

The production branch is `main`. The application source is unchanged by this marker.
